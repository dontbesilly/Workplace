<UserControl x:Class="Workplace1c.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Workplace1c.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <WrapPanel>
        <materialDesign:Card Height="130" Width="330" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                    FontSize="14" Text="Обновление баз телеграмом"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <CheckBox
                        IsChecked="{Binding BotReceiving}"
                        Style="{StaticResource MaterialDesignLightCheckBox}"
                        Content="Запущен" />
                    <Button Margin="8"
                            Command="{Binding StartTelegramCommand}"
                            Style="{StaticResource MaterialDesignRaisedLightButton}"
                            Content="Запустить"/>
                    <Button Margin="8"
                            Command="{Binding StopTelegramCommand}"
                            Style="{StaticResource MaterialDesignRaisedLightButton}"
                            Content="Остановить"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </WrapPanel>
</UserControl>
